# Test-only Python dependencies.
# Install: .venv/bin/pip install -r requirements.txt -r requirements-dev.txt
# Run:     .venv/bin/python -m pytest ml features ingestion -q

pytest==8.4.2
httpx==0.28.1